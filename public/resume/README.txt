Drop your resume PDF here named exactly:

    Prashant_Gupta_Resume.pdf

That filename matches `profile.resumeUrl` in /lib/data.ts, which powers every
"Download Resume" button (navbar, hero, contact). To use a different filename,
update resumeUrl in /lib/data.ts.
